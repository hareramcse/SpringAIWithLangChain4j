services:
    postgres:
        image: postgres:17.6
        container_name: postgres
        ports:
            - "5432:5432"
        restart: always
        environment:
            - POSTGRES_PASSWORD=password
            - POSTGRES_USER=postgres
            - POSTGRES_DB=postgres
