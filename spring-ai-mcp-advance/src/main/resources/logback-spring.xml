<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
      STDIO MCP: never log to stdout/stderr (that pipe is JSON-RPC for the host).

      Use an absolute directory: MCP clients often start java with cwd System32,
      so a relative path like "logs/..." becomes C:\Windows\System32\logs\... and fails.
    -->
    <property name="MCP_LOG_FILE"
              value="${mcp.log.file:-${user.home}/.spring-ai-mcp-advance/logs/mcp-server.log}" />

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${MCP_LOG_FILE}</file>
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.hs" level="INFO" />

    <root level="WARN">
        <appender-ref ref="FILE" />
    </root>
</configuration>
